import axios from "axios";

async function Profile() {
  // better way to do data fetching in nextjs and to make use of server side rendering, make direct axios reqs here instead of wrapping it in useeffect

  return (
    <>
      <div>User Profile</div>
    </>
  );
}

export default Profile;
